# SoundFonts

On first piano render, **TimGM6mb.sf2** (~6 MB) is downloaded here for FluidSynth.

Install the player on Fedora:

```bash
sudo dnf install fluidsynth
```

Without FluidSynth, the app uses an enhanced pluck synthesizer with reverb.
